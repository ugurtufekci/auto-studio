# secrets — never commit
.env
.env.*
!.env.example

# generated media and runtime state
assets/
store/*.db
store/*.log
store/bsky_session.txt
store/bsky_logins.json

# python
__pycache__/
*.pyc
*.pyo
.venv/
.pytest_cache/
.ruff_cache/

# os
.DS_Store
store/instagram_token.json
assets/pinned-intro/
assets/nursery-build/
data/draws/

# per-machine forensics: which browser asked this console to publish.
# Never shared: two machines rewriting one capped jsonl through the ledger
# push is a merge conflict during a release, which is the worst possible time.
data/drafts/release-log.jsonl
