# autoStudio content cycle — 2026-08-28 (14:20 UTC)

Draft-only production run, as designed: no platform credentials on this
machine, every active account in `publish_mode: approve`. Nothing was
published. **Outcome reversed from recent runs: Mara DRAFTED (TTS recovered);
June FAILED on a code error.**

## Result summary

- **Mara — Telegram: DRAFTED** — `20260828-1411-mara-telegram-dc353b`. The fal
  TTS voiceover succeeded this run (no error), ending a run of failures — the
  TTS outage is confirmed **intermittent** (worked 08-19 and today, failed the
  eight runs in between).
- **June — Instagram: FAILED** — the Material Board render raised
  `list index out of range` after a base-room re-render. No draft. Details
  below — this is a new, code-level failure (not the material quality gate).
- **Pruned** two pending drafts older than 7 days (see Ledger pruning).
- Both Bluesky legs correctly gate-blocked as `retired` (not failures).
- Reply tray (Step 1b, June): no Instagram credentials — normal quiet tray.
- Pools fresh (harvest 2026-08-28 13:23); both personas drew new signals.

## Steps attempted

1. Ensured gitignored `.env` bootstrap (FAL_KEY only; `chmod 600`; confirmed
   ignored — not committed, not logged). FAL_KEY was not in the environment.
2. `git fetch origin main && git checkout main && git reset --hard origin/main`
   → HEAD at `8b0eccc` (fresh harvest landed today). `.env` preserved.
3. Setup: deps present in `/tmp/libs`; `ffmpeg` present (6.1.1);
   `PYTHONPATH=/tmp/libs:.` for all python calls.
4. Ran each persona exactly once, oldest first (Mara before June). June failed;
   not retried (a real failure is a finding, per the run-sheet).
5. Step 1b — reply-tray refresh for June (active Instagram): returned the
   no-credentials error (expected). Nothing written to `data/replies`.
6. Pruned two >7-day pending records and their media; committed Mara's draft
   under `data/drafts`; committed this report; pushed.

## Per-persona outcomes

### Mara (food-drink) — cycle #32 — DRAFTED
- **bluesky → gate-blocked (`retired`)**: correct; not retried.
- **telegram → DRAFTED**: `20260828-1411-mara-telegram-dc353b` (`.mp4`,
  slideshow; fade cut 3.5s/frame).
  - Signal: "Autumn comfort-cooking pivot" (fresh pool).
  - **Voiceover assembled with no TTS error** this run — the fal TTS path that
    failed the last several cycles worked today. It is intermittent, not
    permanently down.
  - Media provenance: `local-placeholder` (licensed stock unavailable; never
    paid generation, per Mara's `media_source: stock` config).

### June (home-interiors) — cycle #33 — FAILED
- **bluesky → gate-blocked (`retired`)**: correct; not retried.
- **instagram → FAILED** (no draft). Signal "Cozy corners / cozymaxxing"
  (0.88), format `slideshow_video`, Material Board style. Terminal lines:
  ```
  [14:15:33] generating 4 prompt(s) × 1 candidate(s) · comparison set,
             shared seed 1787926533…
  [14:15:33]   edit mode: 1 base room + 3 edited schemes (the room cannot drift)
  [14:17:20]   base room failed the object-logic look: dead mirror symmetry —
               one re-render
  [14:19:55]   prompt 0: judge picked candidate 0 (single render — nothing to
               choose between)
  [14:19:55] FAILED: list index out of range
  ```
  - **Not a network error.** fal image generation was proceeding; the crash is
    a Python `IndexError` in the render/judge pipeline. It occurred right after
    the base room was re-rendered for "dead mirror symmetry" (the object-logic
    check added in the 2026-08-24 render changes) — the schemes list appears to
    desync after that re-render, so indexing past prompt 0 goes out of range.
  - **Operator/code finding:** the base-room re-render path in Material Board
    can crash the whole cycle with `list index out of range`. Reproducible frames
    are under `assets/run-*` (gitignored). I did not modify the pipeline or
    retry the persona.

## Failed network calls

None this run. Mara's fal TTS succeeded; June's failure was a local `IndexError`
in the render/judge code, not a network call.

## Ledger pruning

**Pruned 2 pending records + their media** (older than 7 days), both June drafts
from 2026-08-20 (7.99 days):
- `20260820-1414-june-instagram-cd98b0` (reel) — media `-cover.jpg`, `.mp4`.
- `20260820-1414-june-instagram-7bc216` (carousel) — media `.jpg` + slides 2–4.

(6 media files; 8 files removed with the 2 JSON records.) No `resolved/` record
is older than 30 days (oldest resolved_at is 2026-08-13).

## Operator action

**11 drafts now wait for approval** in the console's Approvals tab (1 Mara + 10
June) after adding Mara's draft and pruning 2 stale June drafts. Mara is back in
the queue after the TTS recovery; June produced no draft this run.

Two findings:
1. **June's Material Board can crash on the base-room re-render** with
   `list index out of range` (new, code-level — likely from the 2026-08-24
   object-logic render changes). Worth a code look; June will intermittently
   produce no draft until fixed.
2. **Mara's fal TTS is intermittent** — it recovered today after ~8 failed
   runs. A Linux-capable TTS fallback (the current `say` fallback is macOS-only)
   would stop the intermittent outages from zeroing out Mara's drafts.
