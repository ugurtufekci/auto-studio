# autoStudio content cycle — 2026-08-21 (14:14 UTC)

Draft-only production run, as designed: no platform credentials on this
machine, every active account in `publish_mode: approve`. Nothing was
published. **Mixed outcome: June drafted (2); Mara FAILED again on TTS.**

## Result summary

- **June — Instagram: DRAFTED (2)** — a silent vertical Reel plus a 10-slide
  carousel twin, **Material Board** style, from real fal.ai renders
  (`fal-ai/z-image/turbo`, style `june-v4`). All schemes survived the quality
  gate — the 2026-08-19 flat-colour regression appears fixed.
- **Mara — Telegram: FAILED** — the same TTS failure recurred (now 3 of the
  last 4 runs). fal TTS models unavailable, then the macOS-only `say` fallback
  aborts on this Linux runner. No draft. Details below.
- Both Bluesky legs correctly gate-blocked as `retired` (not failures).
- Reply tray (Step 1b, June): no Instagram credentials — normal quiet tray.
- Pools fresh (harvest 2026-08-21 13:18, 7 signals/pool); both personas drew
  new autumn-nesting signals.

## Steps attempted

1. Ensured gitignored `.env` bootstrap (FAL_KEY only; `chmod 600`; confirmed
   ignored — not committed, not logged). FAL_KEY was not in the environment.
2. `git fetch origin main && git checkout main && git reset --hard origin/main`
   → HEAD at `ac7b98a` (fresh harvest + pool-reader robustness fixes landed
   today). `.env` preserved.
3. Setup: deps present in `/tmp/libs`; `ffmpeg` present (6.1.1);
   `PYTHONPATH=/tmp/libs:.` for all python calls.
4. Ran each persona exactly once, oldest first (Mara before June). Mara failed;
   not retried (a real failure is a finding, per the run-sheet).
5. Step 1b — reply-tray refresh for June (active Instagram): returned the
   no-credentials error (expected). Nothing written to `data/replies`.
6. Committed June's 2 drafts under `data/drafts`; committed this report; pushed.

## Per-persona outcomes

### Mara (food-drink) — cycle #18 — FAILED
- **bluesky → gate-blocked (`retired`)**: correct; not retried.
- **telegram → FAILED** during slideshow assembly. No draft. Signal was a
  late-summer fermentation/preserving topic (fresh pool), format
  `slideshow_video`; 4 placeholder frames rendered (stock unavailable), then the
  voiceover step broke. Terminal lines:
  ```
  [14:07:35] voiceover + ffmpeg slideshow assembly…
    [factory] TTS provider unavailable (all tts models failed, last:
      [{'loc': ['body', 'prompt'], 'msg': 'Prom) — local fallback
  [14:07:36] FAILED: [Errno 2] No such file or directory: 'say'
  ```
  - **Failed network call:** the fal TTS request(s) were rejected — the run log
    surfaced only a truncated validation error `[{'loc': ['body', 'prompt'],
    'msg': 'Prom` (a `body.prompt` field validation error from the fal TTS
    endpoint). The factory does not print the request URL or full response body,
    so the exact URL/full text is not recoverable from the run log.
  - **Hard-stop cause:** when all fal TTS models fail, the local fallback shells
    out to macOS `say`, absent on Linux → `[Errno 2] No such file or directory:
    'say'`.
  - **Pattern (persistent, not transient):** this exact failure occurred
    2026-08-18, cleared 2026-08-19, and recurred 2026-08-20 and 2026-08-21 —
    3 of the last 4 runs. A `body.prompt` validation error recurring this
    consistently reads like a request-schema mismatch in the fal TTS call
    rather than pure upstream flakiness. Either way, whenever fal TTS is down
    Mara hard-fails because the `say` fallback cannot run on Linux. **This is
    Mara's blocking issue and it is now the norm, not the exception.** I did not
    modify the version-controlled pipeline or retry the persona.

### June (home-interiors) — cycle #19 — DRAFTED (2)
- **bluesky → gate-blocked (`retired`)**: correct; not retried.
- **instagram → DRAFTED (reel)**: `20260821-1413-june-instagram-038782`
  (`.mp4`, silent vertical 9:16 Reel, **Material Board** style, `june-v4`).
  - Signal: "Cozy, warm-lit small interiors … autumn-nesting" (fresh pool).
  - Media provenance: **`fal-ai/z-image/turbo`** (real paid generation
    succeeded), representative source
    `https://v3b.fal.media/files/b/0aa73c76/UWTz1asOJ3QsFSt2wE5pB_tWwIP2N5.png`.
    All material-board schemes passed (colours "pulled back" to targets); none
    dropped — the 2026-08-19 flat-colour regression looks resolved.
- **instagram → DRAFTED (carousel twin)**:
  `20260821-1413-june-instagram-3f91da` (`carousel`, 10 slides at 4:5), the twin
  of the reel from the same paid frames.

## Failed network calls

- **fal TTS (Mara's voiceover)** — rejected with a `body.prompt` validation
  error; the run log truncated it to `[{'loc': ['body', 'prompt'], 'msg':
  'Prom`. The factory did not log the request URL or full response body, so the
  exact URL/full text is not available from this run's output.
- June's fal **image** generations all succeeded (representative URL above).

## Ledger pruning

Nothing pruned. Oldest `pending/` record is 2026-08-15 (~6 days by its draft
date — under the 7-day threshold; it crosses on 2026-08-22 if still unreleased).
Oldest `resolved/` record is 2026-08-12 (~9 days — under 30). (Filesystem
mtimes are unreliable here because git rewrites them on checkout, so age is
judged by the draft's own date.)

## Operator action

**17 drafts now wait for approval** in the console's Approvals tab (1 Mara + 16
June). This run added June's reel (`...-038782`) and its 10-slide carousel twin
(`...-3f91da`); Mara produced no draft.

Standing item (now urgent): **Mara's TTS path fails on most runs.** fal TTS
returns a `body.prompt` validation error and the `say` fallback is macOS-only,
so Mara has produced no Telegram draft on 3 of the last 4 cycles. Needs a code
fix — correct the fal TTS request, add a Linux-capable TTS fallback, or give
Mara a silent-slideshow path like June's. The queue is also lopsided (16 June
vs 1 Mara) as a direct result.
