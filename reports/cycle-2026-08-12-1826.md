# autoStudio content cycle — 2026-08-12 (18:26 UTC)

First automatic production run of the studio, and the end-to-end proof of the
whole pipeline (pool → brief → media → judge → draft → git). Draft-only, as
designed: no platform credentials on this machine, every active account in
`publish_mode: approve`. Nothing was published.

## Result summary

- **2 drafts produced**, both waiting in `data/drafts/pending/` for the
  operator's Approvals queue.
- **June — Instagram: DRAFTED** with real fal.ai image generation
  (`fal-ai/z-image/turbo`). This is the pipeline's live proof.
- **Mara — Telegram: DRAFTED** (media fell back to the local placeholder —
  licensed stock was unavailable; never paid generation, by config).
- Both personas' Bluesky legs were correctly gate-blocked (not failures).

## Steps attempted

1. Wrote gitignored `.env` bootstrap (FAL_KEY only; `chmod 600`; confirmed
   `git check-ignore` reports it ignored — not committed, not logged).
2. `git fetch origin main && git checkout main && git reset --hard origin/main`
   → HEAD at `041c58d` (fresh main; `.env` preserved).
3. `pip install --quiet --target /tmp/libs -r requirements.txt` (exit 0);
   `PYTHONPATH=/tmp/libs:.` exported for all python calls.
4. Verified fresh pools: snapshot `20260812-1325` is `latest.json` for both
   `home-interiors` (June) and `food-drink` (Mara), ~5h old.
5. **Environment fix:** `ffmpeg` was not installed (needed for Mara's
   `slideshow_video` assembly). `apt-get update && apt-get install -y
   --no-install-recommends ffmpeg` → ffmpeg 6.1.1 installed.
6. Ran each persona exactly once to a terminal outcome (see below).
7. Committed `data/drafts` to main; committed this report; pushed.

Ordering: "oldest persona first" — Mara (accounts opened 2026-08-06) ran
before June (opened 2026-08-10).

Note on Mara's run: the first foreground invocation was killed by a 2-minute
shell timeout mid-render (no draft written, no ledger record — verified). It
was re-run once to completion in the background; the killed attempt was a
harness timeout, not a guardrail or a completed cycle. Only one draft-producing
run occurred per persona.

## Per-persona outcomes

### Mara (food-drink) — cycle #2
- **bluesky → gate-blocked (account status `suspended`)**: "account status is
  'suspended' in config/accounts.yaml — publishing is blocked until it reads
  'active'". AccountTakedown; correct, not retried.
- **telegram → DRAFTED**: `20260812-1823-mara-telegram-62b612`
  (media: `.mp4`, slideshow).
  - Signal chosen: "Late-summer produce & fruit desserts" (seasonal, 0.76).
  - Format: `slideshow_video`. Media provenance: `local-placeholder` —
    licensed stock (Pexels) was unavailable, so the factory used the local
    placeholder rather than paid generation, by Mara's `media_source: stock`
    config. No explicit URL/error string was emitted for the stock lookup in
    the run log; it surfaced only as
    "`[factory] stock-first requested but stock unavailable — using the local
    placeholder, never paid generation`".
  - TTS voiceover + ffmpeg slideshow assembled successfully; telegram
    rendition adapted and held for approval.

### June (home-interiors) — cycle #3
- **bluesky → gate-blocked (account status `paused`)**: "account status is
  'paused' in config/accounts.yaml — publishing is blocked until it reads
  'active'". Deliberate hold pending Mara's Bluesky appeal; correct.
- **instagram → DRAFTED**: `20260812-1826-june-instagram-99baf9`
  (media: `.jpg`, single image).
  - Instagram warm-up gate was **open** (warm-up ended 2026-08-12 00:00 UTC) —
    this is the studio's first automatic draft, as expected.
  - Signal chosen: "Playful maximalism: animal rugs & all-colour rooms"
    (aesthetic, 0.80).
  - Format: `image_post`. Media provenance: **`fal-ai/z-image/turbo`** (real
    paid generation), source
    `https://v3b.fal.media/files/b/0aa612a6/Y_1eH80Yo0Fx3nmjjii_k_siPAFVQz.png`,
    style stamp `june-v1`. 2 candidates generated, judge picked candidate 1.
  - Voice-contract linter and real-subject leak check both passed on the first
    brief (no regeneration needed).

## Failed network calls

None. June's fal.ai generation succeeded (see source URL above). Mara's stock
source was unavailable but did not emit a URL or error string in the run log;
it was handled by the designed local-placeholder fallback, not a hard failure.

## Ledger pruning

Nothing pruned — this is the first run; `data/drafts/pending/` had no records
>7 days old and no `resolved/` directory exists yet.

## Operator action

**2 drafts now wait for approval** in the console's Approvals tab:
- `20260812-1826-june-instagram-99baf9` (June · Instagram · image)
- `20260812-1823-mara-telegram-62b612` (Mara · Telegram · slideshow video)
