# autoStudio content cycle — 2026-08-26 (14:13 UTC)

Draft-only production run, as designed: no platform credentials on this
machine, every active account in `publish_mode: approve`. Nothing was
published. **Mixed outcome: June drafted (2); Mara FAILED again on TTS.**

## Result summary

- **June — Instagram: DRAFTED (2)** — a silent vertical Reel plus a 10-slide
  carousel twin, **Material Board** style, from real fal.ai renders
  (`fal-ai/z-image/turbo`, style `june-v4`). One scheme dropped by the quality
  gate; enough survived to ship.
- **Mara — Telegram: FAILED** — the same TTS failure recurred (now 8 of the
  last 9 runs). No draft. Details below.
- **Pruned** six pending drafts older than 7 days (see Ledger pruning).
- Both Bluesky legs correctly gate-blocked as `retired` (not failures).
- Reply tray (Step 1b, June): no Instagram credentials — normal quiet tray.

## Notable conditions this run

- **Pools 24.6h stale.** No trend-harvest since 2026-08-25 13:25; the cycle
  logged the stale warning. Signals still within their freshness window, so
  June drafted, but she drew the same top signal as yesterday ("Bold color
  maximalism returns"). Just one cycle ahead of today's harvest — not a real
  outage.

## Steps attempted

1. Ensured gitignored `.env` bootstrap (FAL_KEY only; `chmod 600`; confirmed
   ignored — not committed, not logged). FAL_KEY was not in the environment.
2. `git fetch origin main && git checkout main && git reset --hard origin/main`
   → HEAD at `8bb37ec`. `.env` preserved.
3. Setup: deps present in `/tmp/libs`; `ffmpeg` present (6.1.1);
   `PYTHONPATH=/tmp/libs:.` for all python calls.
4. Ran each persona exactly once, oldest first (Mara before June). Mara failed;
   not retried (a real failure is a finding, per the run-sheet).
5. Step 1b — reply-tray refresh for June (active Instagram): returned the
   no-credentials error (expected). Nothing written to `data/replies`.
6. Pruned six >7-day pending records and their media; committed `data/drafts`;
   committed this report; pushed.

## Per-persona outcomes

### Mara (food-drink) — cycle #28 — FAILED
- **bluesky → gate-blocked (`retired`)**: correct; not retried.
- **telegram → FAILED** during slideshow assembly. No draft. Format
  `slideshow_video`; placeholder frames rendered (stock unavailable), then the
  voiceover step broke. Terminal lines:
  ```
  [14:06:31] voiceover + ffmpeg slideshow assembly…
    [factory] TTS provider unavailable (all tts models failed, last:
      [{'loc': ['body', 'prompt'], 'msg': 'Prom) — local fallback
  [14:06:31] FAILED: [Errno 2] No such file or directory: 'say'
  ```
  - **Failed network call:** the fal TTS request(s) were rejected — the run log
    surfaced only a truncated validation error `[{'loc': ['body', 'prompt'],
    'msg': 'Prom` (a `body.prompt` field validation error from the fal TTS
    endpoint). The factory does not print the request URL or full response body,
    so the exact URL/full text is not recoverable from the run log.
  - **Hard-stop cause:** all fal TTS models fail → local fallback shells out to
    macOS `say`, absent on Linux → `[Errno 2] No such file or directory: 'say'`.
  - **Pattern (persistent):** 8 of the last 9 runs (only 2026-08-19 succeeded).
    The automated cycle's Mara leg is effectively down; the console path can
    still make Mara drafts.

### June (home-interiors) — cycle #29 — DRAFTED (2)
- **bluesky → gate-blocked (`retired`)**: correct; not retried.
- **instagram → DRAFTED (reel)**: `20260826-1412-june-instagram-a65880`
  (`.mp4`, silent vertical 9:16 Reel, **Material Board** style, `june-v4`).
  - Signal: "Bold color maximalism returns" (same as 08-25 — 24.6h pool).
  - Media provenance: **`fal-ai/z-image/turbo`** (real paid generation
    succeeded), representative source
    `https://v3b.fal.media/files/b/0aa7e531/BN9jmxO7qwTRndEsQ3nRF_2kIkNUTK.png`.
    Scheme 5 dropped by the quality gate ("still looks like the base room");
    the remaining schemes shipped.
- **instagram → DRAFTED (carousel twin)**:
  `20260826-1412-june-instagram-070e14` (`carousel`, 10 slides at 4:5), twin of
  the reel.

## Failed network calls

- **fal TTS (Mara's voiceover)** — rejected with a `body.prompt` validation
  error; the run log truncated it to `[{'loc': ['body', 'prompt'], 'msg':
  'Prom`. The factory did not log the request URL or full response body, so the
  exact URL/full text is not available from this run's output.
- June's fal **image** generations all succeeded (representative URL above).

## Ledger pruning

**Pruned 6 pending records + their media** (older than 7 days), all operator
console-generated June drafts from 2026-08-19 morning (7.08–7.14 days):
`20260819-1039-…-12c280`, `…-4c860f`, `20260819-1155-…-6ed590`, `…-775d45`,
`20260819-1204-…-03cbe8`, `…-203a1d` — 19 media files in total (25 files
removed with the 6 JSON records).

Borderline kept: `20260819-1409-mara-telegram-b2dc44` (created 14:09) sat just
on the keep side of the 7-day cutoff at this run's reference time (14:04); it
becomes prunable next cycle if still unreleased. Only these six morning records
were pruned — the afternoon 2026-08-19 drafts remain. No `resolved/` record is
older than 30 days (oldest resolved_at is 2026-08-13).

## Operator action

**13 drafts now wait for approval** in the console's Approvals tab (1 Mara + 12
June) after adding today's 2 June drafts and pruning 6 stale June drafts. This
run added June's reel (`...-a65880`) and its 10-slide carousel twin
(`...-070e14`); Mara produced no draft.

Standing item (unresolved, 8 of 9 runs): **the automated cycle's Mara leg is
down on TTS.** fal TTS returns a `body.prompt` validation error and the `say`
fallback is macOS-only. The console path can still make Mara drafts, but the
daily cycle cannot until this is fixed (correct the fal TTS request, add a
Linux-capable TTS fallback, or give Mara a silent-slideshow path like June's).
