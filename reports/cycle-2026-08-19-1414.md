# autoStudio content cycle — 2026-08-19 (14:14 UTC)

Draft-only production run, as designed: no platform credentials on this
machine, every active account in `publish_mode: approve`. Nothing was
published. **Mixed outcome: Mara drafted (recovered); June FAILED on the
Material Board quality gate.**

## Result summary

- **Mara — Telegram: DRAFTED** — `20260819-1409-mara-telegram-b2dc44`. Yesterday
  Mara failed on TTS; today the voiceover step succeeded cleanly, so
  2026-08-18's fal TTS error was a transient upstream outage, now recovered.
- **June — Instagram: FAILED** — the Material Board style dropped 4 of 5
  schemes at the quality gate (surfaces rendered as one flat colour instead of
  distinct walls/cabinets/floor); only 2 survived and a comparison needs ≥3, so
  the cycle hard-failed with no draft. Likely a regression from today's
  walls-first materials-mode changes. Details below.
- Both Bluesky legs correctly gate-blocked as `retired` (not failures).
- Reply tray (Step 1b, June): no Instagram credentials — normal quiet tray.

## Notable conditions this run

- **Signal pools are 120.7h stale — FIFTH content-day on the same snapshot.**
  Latest pool snapshot is still 2026-08-14 13:25; trend-harvest has not
  produced a new one. Cycle logged the stale warning for both pools. Signals
  are still within their freshness window, so Mara drafted, but again on the
  same top signal ("Homemade baking surge"). **Fourth report flagging
  trend-harvest as stopped — please re-run / repair it.**
- **Material Board regression (June).** Today's pipeline changes ("materials-
  mode test follows the walls-first instruction"; "the room is drawn too, and
  the walls are named first") appear to have regressed the Material Board
  render: for schemes 2–5 the generated room "came back as one flat colour
  instead of walls, cabinets and floor", so each failed the material-diversity
  check (label promises 4 materials spanning 0.63 in lightness; rooms measured
  0.07–0.22). After one retry each, 4 of 5 schemes were dropped, leaving 2 —
  below the 3-scheme comparison floor — and the cycle aborted. This gate is
  working as designed (it refused to ship a broken comparison); the underlying
  render regression is the operator/code finding.

## Steps attempted

1. Ensured gitignored `.env` bootstrap (FAL_KEY only; `chmod 600`; confirmed
   ignored — not committed, not logged). FAL_KEY was not in the environment.
2. `git fetch origin main && git checkout main && git reset --hard origin/main`
   → HEAD at `2ee7b6b`. `.env` preserved.
3. Setup: deps present in `/tmp/libs`; `ffmpeg` present (6.1.1); `say` absent
   (macOS-only, as expected on Linux); `PYTHONPATH=/tmp/libs:.` for all python.
4. Ran each persona exactly once, oldest first (Mara before June). June failed;
   not retried (a real failure is a finding, per the run-sheet).
5. Step 1b — reply-tray refresh for June (active Instagram): returned the
   no-credentials error (expected). Nothing written to `data/replies`.
6. Committed Mara's draft under `data/drafts`; committed this report; pushed.

## Per-persona outcomes

### Mara (food-drink) — cycle #14 — DRAFTED
- **bluesky → gate-blocked (`retired`)**: correct; not retried.
- **telegram → DRAFTED**: `20260819-1409-mara-telegram-b2dc44` (`.mp4`,
  slideshow; comparison set, shared seed 1787148547, 5 spec-labelled frames,
  fade cut 3.5s/frame).
  - Signal: "Homemade baking surge" (0.94) — same top signal (stale pool);
    fresh caption.
  - Voiceover assembled with **no TTS error** this run (yesterday's fal TTS
    outage has cleared).
  - Media provenance: `local-placeholder` (licensed stock unavailable; never
    paid generation, per config) — surfaced only as "`[factory] stock-first
    requested but stock unavailable — using the local placeholder, never paid
    generation`".

### June (home-interiors) — cycle #15 — FAILED
- **bluesky → gate-blocked (`retired`)**: correct; not retried.
- **instagram → FAILED** (no draft). Signal "Houseplant blooms & propagation"
  (0.89), format `slideshow_video`, Material Board style. Terminal lines:
  ```
    [factory] scheme 5: the label promises 4 materials spanning 0.63 in
      lightness and the room shows 0.07 — the surfaces came back as one flat
      colour instead of walls, cabinets and floor
  [14:14:26]   DROPPED scheme 2 / 3 / 4 / 5 (same flat-colour reason)
  [14:14:31]   image source: fal-ai/z-image/turbo
  [14:14:31] FAILED: only 2 schemes survived the quality gates — a comparison
    needs at least 3. Frames are in
    /home/user/auto-studio/assets/run-20260819-141013 and the reasons are
    above; nothing was queued.
  ```
  - fal **image** generation itself succeeded (`fal-ai/z-image/turbo`); the
    failure is the post-render material-diversity quality gate, not a network
    error.

## Failed network calls

None this run. Mara's fal TTS succeeded; June's fal image generations
succeeded. June's failure was a local quality-gate rejection, not a network
error.

## Ledger pruning

Nothing pruned. Oldest `pending/` record is 2026-08-15 (under 7 days); oldest
`resolved/` record is under 30 days.

## Operator action

**7 drafts now wait for approval** in the console's Approvals tab (1 Mara + 6
June). This run added Mara's `...-b2dc44`; June produced no draft.

Two standing items:
1. **June's Material Board render regressed today** — schemes come back as a
   single flat colour and fail the material-diversity gate, so June cannot
   ship a Material Board comparison. Likely from today's walls-first
   materials-mode changes; needs a code look.
2. **trend-harvest has not run for ~5 days** — pools are stuck on the
   2026-08-14 snapshot. Please re-run / repair it.
