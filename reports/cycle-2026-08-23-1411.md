# autoStudio content cycle — 2026-08-23 (14:11 UTC)

Draft-only production run, as designed: no platform credentials on this
machine, every active account in `publish_mode: approve`. Nothing was
published. **Mixed outcome: June drafted (2); Mara FAILED again on TTS.**
First run with an actual ledger prune.

## Result summary

- **June — Instagram: DRAFTED (2)** — a silent vertical Reel plus a 6-slide
  carousel twin, **Material Board** style, from real fal.ai renders
  (`fal-ai/z-image/turbo`, style `june-v4`). One over-saturated scheme was
  dropped by the quality gate; enough survived to ship.
- **Mara — Telegram: FAILED** — the same TTS failure recurred (now 5 of the
  last 6 runs). No draft. Details below.
- **Pruned** one pending draft older than 7 days (see Ledger pruning).
- Both Bluesky legs correctly gate-blocked as `retired` (not failures).
- Reply tray (Step 1b, June): no Instagram credentials — normal quiet tray.

## Notable conditions this run

- **Pools mildly stale (33.8h).** No new trend-harvest fired yet today; latest
  snapshot is 2026-08-22 04:16. Signals still within their freshness window, so
  June drafted, but she drew the same top signal as yesterday ("Fall 2026
  jewel-tone palettes"). Not the multi-day staleness of last week — just this
  cycle running ahead of today's harvest. Fresh imagery either way.

## Steps attempted

1. Ensured gitignored `.env` bootstrap (FAL_KEY only; `chmod 600`; confirmed
   ignored — not committed, not logged). FAL_KEY was not in the environment.
2. `git fetch origin main && git checkout main && git reset --hard origin/main`
   → HEAD at `2059804` (console changes: read production history from the git
   ledger). `.env` preserved.
3. Setup: deps present in `/tmp/libs`; `ffmpeg` present (6.1.1);
   `PYTHONPATH=/tmp/libs:.` for all python calls.
4. Ran each persona exactly once, oldest first (Mara before June). Mara failed;
   not retried (a real failure is a finding, per the run-sheet).
5. Step 1b — reply-tray refresh for June (active Instagram): returned the
   no-credentials error (expected). Nothing written to `data/replies`.
6. Pruned one >7-day pending record and its media; committed `data/drafts`;
   committed this report; pushed.

## Per-persona outcomes

### Mara (food-drink) — cycle #22 — FAILED
- **bluesky → gate-blocked (`retired`)**: correct; not retried.
- **telegram → FAILED** during slideshow assembly. No draft. Format
  `slideshow_video`; placeholder frames rendered (stock unavailable), then the
  voiceover step broke. Terminal lines:
  ```
  [14:05:48] voiceover + ffmpeg slideshow assembly…
    [factory] TTS provider unavailable (all tts models failed, last:
      [{'loc': ['body', 'prompt'], 'msg': 'Prom) — local fallback
  [14:05:49] FAILED: [Errno 2] No such file or directory: 'say'
  ```
  - **Failed network call:** the fal TTS request(s) were rejected — the run log
    surfaced only a truncated validation error `[{'loc': ['body', 'prompt'],
    'msg': 'Prom` (a `body.prompt` field validation error from the fal TTS
    endpoint). The factory does not print the request URL or full response body,
    so the exact URL/full text is not recoverable from the run log.
  - **Hard-stop cause:** all fal TTS models fail → local fallback shells out to
    macOS `say`, absent on Linux → `[Errno 2] No such file or directory: 'say'`.
  - **Pattern (persistent):** ✗08-18, ✓08-19, ✗08-20, ✗08-21, ✗08-22, ✗08-23 —
    5 of the last 6 runs. Mara's standing blocker; no pipeline edit or retry.

### June (home-interiors) — cycle #23 — DRAFTED (2)
- **bluesky → gate-blocked (`retired`)**: correct; not retried.
- **instagram → DRAFTED (reel)**: `20260823-1411-june-instagram-61f87f`
  (`.mp4`, silent vertical 9:16 Reel, **Material Board** style, `june-v4`).
  - Signal: "Fall 2026 jewel-tone palettes" (same as 08-22 — 33.8h-old pool).
  - Media provenance: **`fal-ai/z-image/turbo`** (real paid generation
    succeeded), representative source
    `https://v3b.fal.media/files/b/0aa77fed/L5Vk2-7mNLawRXJK6Hmqi_7MXABuoX.png`.
    Scheme 4 dropped by the quality gate ("every surface is the same saturated
    colour"); the remaining schemes shipped.
- **instagram → DRAFTED (carousel twin)**:
  `20260823-1411-june-instagram-18ae27` (`carousel`, 6 slides at 4:5), twin of
  the reel.

## Failed network calls

- **fal TTS (Mara's voiceover)** — rejected with a `body.prompt` validation
  error; the run log truncated it to `[{'loc': ['body', 'prompt'], 'msg':
  'Prom`. The factory did not log the request URL or full response body, so the
  exact URL/full text is not available from this run's output.
- June's fal **image** generations all succeeded (representative URL above).

## Ledger pruning

**Pruned 1 pending record + its media** (older than 7 days):
- `20260815-1734-june-instagram-6fa5ea` (created 2026-08-15T17:34Z, 7.85 days
  old) and its media `20260815-1734-june-instagram-6fa5ea.mp4`.

The next-oldest pending records are the two `20260817-0814` June drafts (6.24
days) — under 7 days, kept; they become prunable ~2026-08-24. No `resolved/`
record is older than 30 days (oldest resolved_at is 2026-08-13), so nothing
pruned there.

## Operator action

**19 drafts now wait for approval** in the console's Approvals tab (1 Mara + 18
June) after adding today's 2 June drafts and pruning 1 stale June draft. This
run added June's reel (`...-61f87f`) and its 6-slide carousel twin
(`...-18ae27`); Mara produced no draft.

Standing item (unresolved, 5 of 6 runs): **Mara's TTS path fails on most runs.**
fal TTS returns a `body.prompt` validation error and the `say` fallback is
macOS-only, so Mara produces no Telegram draft — the queue is now 18 June vs 1
Mara. Needs a code fix (correct the fal TTS request, add a Linux-capable TTS
fallback, or give Mara a silent-slideshow path like June's).
