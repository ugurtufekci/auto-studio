# autoStudio content cycle — 2026-08-27 (14:13 UTC)

Draft-only production run, as designed: no platform credentials on this
machine, every active account in `publish_mode: approve`. Nothing was
published. **Mixed outcome: June drafted (2); Mara FAILED again on TTS.**

## Result summary

- **June — Instagram: DRAFTED (2)** — a silent vertical Reel plus an 8-slide
  carousel twin, **Material Board** style, from real fal.ai renders
  (`fal-ai/z-image/turbo`, style `june-v4`). Fresh signal from today's harvest.
- **Mara — Telegram: FAILED** — the same TTS failure recurred (now 9 of the
  last 10 runs). No draft.
- **Pruned** three pending drafts older than 7 days, including the last Mara
  draft — the approval queue is now **all June, zero Mara** (see below).
- Both Bluesky legs correctly gate-blocked as `retired` (not failures).
- Reply tray (Step 1b, June): no Instagram credentials — normal quiet tray.
- Pools fresh (harvest 2026-08-27 13:18, 7–8 signals/pool); June drew a new
  signal ("Fall 2026 interior trend roundups").

## Steps attempted

1. Ensured gitignored `.env` bootstrap (FAL_KEY only; `chmod 600`; confirmed
   ignored — not committed, not logged). FAL_KEY was not in the environment.
2. `git fetch origin main && git checkout main && git reset --hard origin/main`
   → HEAD at `f92bf99` (fresh harvest landed today). `.env` preserved.
3. Setup: deps present in `/tmp/libs`; `ffmpeg` present (6.1.1);
   `PYTHONPATH=/tmp/libs:.` for all python calls.
4. Ran each persona exactly once, oldest first (Mara before June). Mara failed;
   not retried (a real failure is a finding, per the run-sheet).
5. Step 1b — reply-tray refresh for June (active Instagram): returned the
   no-credentials error (expected). Nothing written to `data/replies`.
6. Pruned three >7-day pending records and their media; committed `data/drafts`;
   committed this report; pushed.

## Per-persona outcomes

### Mara (food-drink) — cycle #30 — FAILED
- **bluesky → gate-blocked (`retired`)**: correct; not retried.
- **telegram → FAILED** during slideshow assembly. No draft. Terminal lines:
  ```
  [14:06:14] voiceover + ffmpeg slideshow assembly…
    [factory] TTS provider unavailable (all tts models failed, last:
      [{'loc': ['body', 'prompt'], 'msg': 'Prom) — local fallback
  [14:06:15] FAILED: [Errno 2] No such file or directory: 'say'
  ```
  - **Failed network call:** the fal TTS request(s) were rejected — the run log
    surfaced only a truncated validation error `[{'loc': ['body', 'prompt'],
    'msg': 'Prom` (a `body.prompt` field validation error from the fal TTS
    endpoint). The factory does not print the request URL or full response body,
    so the exact URL/full text is not recoverable from the run log.
  - **Hard-stop cause:** all fal TTS models fail → local fallback shells out to
    macOS `say`, absent on Linux → `[Errno 2] No such file or directory: 'say'`.
  - **Pattern (persistent):** 9 of the last 10 runs (only 2026-08-19 succeeded).

### June (home-interiors) — cycle #31 — DRAFTED (2)
- **bluesky → gate-blocked (`retired`)**: correct; not retried.
- **instagram → DRAFTED (reel)**: `20260827-1412-june-instagram-c6898b`
  (`.mp4`, silent vertical 9:16 Reel, **Material Board** style, `june-v4`).
  - Signal: "Fall 2026 interior trend roundups" (fresh pool — new topic).
  - Media provenance: **`fal-ai/z-image/turbo`** (real paid generation
    succeeded), representative source
    `https://v3b.fal.media/files/b/0aa806ef/0z-dkOuVWPvDYnDBqAyKL_BFSS86Uq.png`.
- **instagram → DRAFTED (carousel twin)**:
  `20260827-1412-june-instagram-87f3a9` (`carousel`, 8 slides at 4:5), twin of
  the reel.

## Failed network calls

- **fal TTS (Mara's voiceover)** — rejected with a `body.prompt` validation
  error; the run log truncated it to `[{'loc': ['body', 'prompt'], 'msg':
  'Prom`. The factory did not log the request URL or full response body, so the
  exact URL/full text is not available from this run's output.
- June's fal **image** generations all succeeded (representative URL above).

## Ledger pruning

**Pruned 3 pending records + their media** (older than 7 days):
- `20260819-1409-mara-telegram-b2dc44` (8.0d) — media `-cover.jpg`, `.mp4`.
  This was the **only Mara draft in the queue**; pruning it (unreleased for 8
  days) empties the Mara queue.
- `20260819-2226-june-instagram-687a2d` (7.65d) — media `.jpg` + slides 2–8.
- `20260819-2226-june-instagram-9337f7` (7.65d) — media `-cover.jpg`, `.mp4`.

(12 media files total; 15 files removed with the 3 JSON records.) No `resolved/`
record is older than 30 days (oldest resolved_at is 2026-08-13).

## Operator action

**12 drafts now wait for approval** in the console's Approvals tab — **all
June, 0 Mara** — after adding today's 2 June drafts and pruning 3 stale drafts
(including the last Mara one). This run added June's reel (`...-c6898b`) and its
8-slide carousel twin (`...-87f3a9`); Mara produced no draft.

Standing item (now with a queue consequence): **the automated cycle's Mara leg
has been down on TTS for 9 of the last 10 runs**, and with the last console-made
Mara draft now pruned, there is **no Mara content left to approve**. Until the
TTS path is fixed (correct the fal TTS request, add a Linux-capable TTS
fallback, or give Mara a silent-slideshow path like June's), the Telegram
account will go dark unless the operator keeps generating Mara drafts by hand in
the console.
