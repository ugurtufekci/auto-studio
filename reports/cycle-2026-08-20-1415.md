# autoStudio content cycle — 2026-08-20 (14:15 UTC)

Draft-only production run, as designed: no platform credentials on this
machine, every active account in `publish_mode: approve`. Nothing was
published. **Mixed outcome: June drafted (2); Mara FAILED again on TTS.**

## Result summary

- **June — Instagram: DRAFTED (2)** — a silent vertical Reel plus its carousel
  twin, this time in the **Colourway** style (not Material Board), from real
  fal.ai renders (`fal-ai/z-image/turbo`, style `june-v4`). All 4 schemes
  survived the quality gate.
- **Mara — Telegram: FAILED** — the same TTS failure as 2026-08-18 recurred:
  fal TTS models unavailable, then the macOS-only `say` fallback aborts on this
  Linux runner. No draft. Details below.
- Both Bluesky legs correctly gate-blocked as `retired` (not failures).
- Reply tray (Step 1b, June): no Instagram credentials — normal quiet tray.

## Good news this run

- **Trend-harvest recovered.** Pools are fresh again (snapshot 2026-08-19
  22:19, ~16h old) after five days stuck on 2026-08-14. Both personas drew NEW
  top signals this run — Mara "Late-summer conservas / tinned-fish pantry",
  June "Stained glass & tavern lighting revival" — ending the repeat-signal
  problem flagged in the last four reports.
- **June's Material Board regression avoided.** June's cycle selected the
  Colourway style this run; all schemes passed, so she drafted cleanly. (The
  Material Board flat-colour regression from 2026-08-19 was not exercised this
  run, so it is neither confirmed fixed nor re-observed — worth a check when
  Material Board is next selected.)

## Steps attempted

1. Ensured gitignored `.env` bootstrap (FAL_KEY only; `chmod 600`; confirmed
   ignored — not committed, not logged). FAL_KEY was not in the environment.
2. `git fetch origin main && git checkout main && git reset --hard origin/main`
   → HEAD at `6566142` (fresh harvest landed; Instagram cadence changed — gap
   removed, daily cap raised to 10). `.env` preserved.
3. Setup: deps present in `/tmp/libs`; `ffmpeg` present (6.1.1);
   `PYTHONPATH=/tmp/libs:.` for all python calls.
4. Ran each persona exactly once, oldest first (Mara before June). Mara failed;
   not retried (a real failure is a finding, per the run-sheet).
5. Step 1b — reply-tray refresh for June (active Instagram): returned the
   no-credentials error (expected). Nothing written to `data/replies`.
6. Committed June's 2 drafts under `data/drafts`; committed this report; pushed.

## Per-persona outcomes

### Mara (food-drink) — cycle #16 — FAILED
- **bluesky → gate-blocked (`retired`)**: correct; not retried.
- **telegram → FAILED** during slideshow assembly. No draft. Signal was
  "Late-summer conservas / tinned-fish pantry" (fresh pool), format
  `slideshow_video`; 5 placeholder frames rendered (stock unavailable), then
  the voiceover step broke. Terminal lines:
  ```
  [14:09:32] voiceover + ffmpeg slideshow assembly…
    [factory] TTS provider unavailable (all tts models failed, last:
      [{'loc': ['body', 'prompt'], 'msg': 'Prom) — local fallback
  [14:09:33] FAILED: [Errno 2] No such file or directory: 'say'
  ```
  - **Failed network call:** the fal TTS request(s) were rejected — the run log
    surfaced only a truncated validation error `[{'loc': ['body', 'prompt'],
    'msg': 'Prom` (a `body.prompt` field validation error from the fal TTS
    endpoint). The factory does not print the request URL or full response
    body, so the exact URL/full text is not recoverable from the run log.
  - **Hard-stop cause:** when all fal TTS models fail, the local fallback shells
    out to the macOS `say` binary, absent on this Linux runner → `[Errno 2] No
    such file or directory: 'say'`.
  - **Pattern:** this exact failure occurred on 2026-08-18, cleared on
    2026-08-19, and recurred today. The fal TTS path is intermittent, and
    whenever it is down Mara hard-fails because the `say` fallback cannot run on
    Linux. Until the fallback is fixed (or a Linux TTS is provided), Mara's
    Telegram slideshow is one fal-TTS outage away from producing no draft.
    Operator/code finding — I did not modify the pipeline or retry the persona.

### June (home-interiors) — cycle #17 — DRAFTED (2)
- **bluesky → gate-blocked (`retired`)**: correct; not retried.
- **instagram → DRAFTED (reel)**: `20260820-1414-june-instagram-cd98b0`
  (`.mp4`, silent vertical 9:16 Reel, **Colourway** style, `june-v4`).
  - Signal: "Stained glass & tavern lighting revival" (fresh pool).
  - Media provenance: **`fal-ai/z-image/turbo`** (real paid generation
    succeeded), representative source
    `https://v3b.fal.media/files/b/0aa71aba/8-L1O5pM4c5vPzJ4-Mbb5_otfMzxjg.png`.
    Edit mode: 1 base room + 3 edited schemes; all survived the quality gate.
- **instagram → DRAFTED (carousel twin)**:
  `20260820-1414-june-instagram-7bc216` (`carousel`, 4 slides at 4:5), the twin
  of the reel from the same paid frames.

## Failed network calls

- **fal TTS (Mara's voiceover)** — rejected with a `body.prompt` validation
  error; the run log truncated it to `[{'loc': ['body', 'prompt'], 'msg':
  'Prom`. The factory did not log the request URL or full response body, so the
  exact URL/full text is not available from this run's output.
- June's fal **image** generations all succeeded (representative URL above).

## Ledger pruning

Nothing pruned. Oldest `pending/` record is 2026-08-15 (5 days — under the
7-day threshold, but it will cross it on 2026-08-22 if unreleased); oldest
`resolved/` record is 2026-08-12 (8 days — under 30).

## Operator action

**16 drafts now wait for approval** in the console's Approvals tab (1 Mara + 15
June). This run added June's reel (`...-cd98b0`) and its carousel twin
(`...-7bc216`); Mara produced no draft.

Standing item: **Mara's TTS fallback is broken on Linux.** fal TTS is
intermittent, and the `say` fallback is macOS-only, so any fal-TTS outage makes
Mara fail with no draft (happened 08-18 and again today). Needs a code fix —
either a Linux-capable TTS fallback or a silent-slideshow path for Mara like
June's.
